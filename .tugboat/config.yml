services:
  node:
    image: tugboatqa/node:24
    default: true
    expose: 3000

    init:
      - npm install -g serve pm2

    build:
      - npm ci
      - npm run docs:build

    start:
      - pm2 start "serve -s ${TUGBOAT_ROOT}/docs/.vitepress/dist -l 3000" --name docs || pm2 restart docs
